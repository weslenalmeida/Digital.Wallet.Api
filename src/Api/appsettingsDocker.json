{
  "TokenConfiguration": {
    "Audience": "${TokenConfiguration.Audience}",
    "Issuer": "${TokenConfiguration.Issuer}",
    "Seconds": "${TokenConfiguration.Seconds}",
    "key": "${TokenConfiguration.key}"
  },

  "ConnectionSettings": {
    "ConnectionSqlString": "${ConnectionSettings.ConnectionSqlString}"
  },
  "Salt": "${Salt}",
  "SubscriptionKey": "${SubscriptionKey}"
}